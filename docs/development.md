# Development

`main` is integration. Work on `feat/*`, `fix/*`, `docs/*`, or `ci/*`.

```bash
npm install
npm run check
npm run build
```

PR: focused, documented, CI green. Squash merge. Delete the branch.
